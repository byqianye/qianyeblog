# Do not copy this template directly. The initializer writes .env safely.
# Fresh setup: set ADMIN_USERNAME and ADMIN_PASSWORD, then run `node server/init-admin.mjs`.
# Existing legacy setup: run `node server/init-admin.mjs --migrate` before updating past the legacy admin state.
ADMIN_USERNAME=
ADMIN_PASSWORD_HASH=
